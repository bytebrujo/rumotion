---
image: /generated/articles-docs-cli-ffmpeg.png
id: ffmpeg
title: npx picus ffmpeg
crumb: '@picus/cli'
sidebar_label: 'ffmpeg'
---

_available since v4.0_

In order to use `ffmpeg` without having to directly install it, Picus provides it via `npx picus ffmpeg`.

Note that in order to keep the binary size small, the FFmpeg binary only understand the codecs that Picus itself supports: H.264, H.265, VP8, VP9 and ProRes. A binary from the 7.1 release line of FFmpeg is used.

# Example

Convert a video file to an audio file

```bash
npx picus ffmpeg -i input.mp4 output.mp3
```

To find out more about FFmpeg, visit their [docs](https://ffmpeg.org/documentation.html).

## See also

- [`npx picus ffprobe`](/docs/cli/ffprobe)
