---
image: /generated/articles-docs-angular.png
id: angular
sidebar_label: 'Integration into Angular'
title: 'Using Picus in Angular'
crumb: 'Integrations'
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Using Picus in Angular

This guide explains how to integrate Picus into an **Angular** project.

## Install required packages

Install Picus and necessary dependencies:

<Tabs
defaultValue="npm"
values={[
{ label: 'npm', value: 'npm', },
{ label: 'pnpm', value: 'pnpm', },
{ label: 'yarn', value: 'yarn', },
{ label: 'bun', value: 'bun', },
]
}>
<TabItem value="npm">

```bash title="Use npm as the package manager"
npm i picus @picus/player @picus/cli @picus/zod-types react react-dom zod
npm i --save-dev @types/react @types/react-dom
```

  </TabItem>
  <TabItem value="pnpm">

```bash title="Use pnpm as the package manager"
pnpm i picus @picus/player @picus/cli @picus/zod-types react react-dom zod
pnpm i --dev @types/react @types/react-dom
```

  </TabItem>

  <TabItem value="yarn">

```bash title="Use Yarn as the package manager"
yarn add picus @picus/player @picus/cli @picus/zod-types react react-dom zod
yarn add --dev @types/react @types/react-dom
```

  </TabItem>

  <TabItem value="bun">

```bash title="Use Bun as the package manager and runtime"
bun i picus @picus/player @picus/cli @picus/zod-types react react-dom zod
bun --dev @types/react @types/react-dom
```

:::note
Bun as a runtime is mostly supported. [Read more here](/docs/bun).
:::

  </TabItem>
</Tabs>

## Create a Picus folder

For better separation, create a folder to hold your Picus files:

```plaintext
src/app/picus
```

Copy the contents of your Picus project or a starter template (e.g., HelloWorld) into this new folder. This will help separate Picus related files from the rest of your Angular codebase.

## Copy `picus.config.ts`

Copy the `picus.config.ts` file to the root directory of your Angular project, placing it at the same level as `package.json`.

This configuration file is necessary for Picus to recognize and compile your project settings.

## Configure TypeScript for JSX

To enable JSX support in Angular, update the `tsconfig.json` file by setting `"jsx": "react"` under `compilerOptions`. This configuration allows Angular to interpret JSX syntax used in Picus's React components.

```json title="tsconfig.json"
{
  "compilerOptions": {
    "jsx": "react",
    "skipLibCheck": true // Recommended to avoid errors with certain libraries
    // other options
  }
}
```

:::note
The `"skipLibCheck": true` setting is also recommended to prevent compatibility issues with certain library types.
:::

## Create a React wrapper component for Angular

To embed Picus components in Angular, create a **wrapper component**:

1. In your `picus` folder, create a file named `PlayerViewWrapper.tsx`.
2. Ensure each `.tsx` file imports React explicitly at the top of the file:

```tsx title="PlayerViewWrapper.tsx"
import {
  AfterViewInit,
  Component,
  effect,
  ElementRef,
  EventEmitter,
  Input,
  OnDestroy,
  Output,
  signal,
  Signal,
  ViewChild,
  WritableSignal,
} from '@angular/core';
import {CommonModule} from '@angular/common';
import {RouterOutlet} from '@angular/router';
import React from 'react';
import {createRoot, Root} from 'react-dom/client';
import {PlayerRef} from '@picus/player';
import {myCompSchema, PlayerView} from './PlayerView';
import {z} from 'zod';

const rootDomID: string = 'reactCounterWrapperId';

@Component({
  selector: 'app-player-view',
  standalone: true,
  imports: [CommonModule, RouterOutlet],
  template: ` <div id="${rootDomID}" #${rootDomID}></div>`,
})
export class PlayerViewWrapper implements AfterViewInit, OnDestroy {
  @ViewChild(rootDomID, {static: false}) containerRef: ElementRef | undefined;
  @Input({required: true}) data: Signal<z.infer<typeof myCompSchema>> = signal({
    titleText: 'Welcome to Picus',
    titleColor: '#000000',
    logoColor1: '#91EAE4',
    logoColor2: '#86A8E7',
  });
  @Output() onPaused = new EventEmitter<void>();
  playerRef: WritableSignal<PlayerRef | undefined> = signal(undefined);

  private root?: Root;

  constructor() {
    effect(() => {
      this.render();
    });
  }

  ngAfterViewInit() {
    this.root = createRoot(this.getRootDomNode());
    this.render();
    this.playerRef()?.play();
  }

  ngOnDestroy(): void {
    this.root?.unmount();
  }

  private getRootDomNode() {
    if (!this.containerRef || !this.containerRef.nativeElement) {
      throw new Error('Cannot get root element. This should not happen.');
    }
    return this.containerRef.nativeElement;
  }

  protected render() {
    if (!this.containerRef || !this.containerRef.nativeElement) {
      return;
    }

    this.root?.render(
      <PlayerView
        playerRefInstance={this.playerRef}
        data={this.data()}
        onPaused={() => this.onPaused.emit()}
      />,
    );
  }
}
```

This wrapper component will serve as the bridge between Angular and Picus’s React components.

You can also pass an EventEmitter instead of a Signal.

## Create a wrapper for the Picus player

1. In your `picus` folder, create a file named `PlayerView.tsx`.
2. Ensure each `.tsx` file imports React explicitly at the top of the file:

This will get the reference to the player with `createRef`.

```tsx title="PlayerView.tsx"
import React, {useEffect} from 'react';
import {Player, PlayerRef} from '@picus/player';
import {z} from 'zod';
import {HelloWorld} from './HelloWorld';
import {zColor} from '@picus/zod-types';
import {WritableSignal} from '@angular/core';

export const PlayerView: React.FC<{
  data: z.infer<typeof myCompSchema>;
  playerRefInstance: WritableSignal<PlayerRef | undefined>;
  onPaused?: () => void;
}> = ({data, playerRefInstance, onPaused}) => {
  const playerRef: React.RefObject<PlayerRef> = React.createRef();

  useEffect(() => {
    if (playerRef.current) {
      playerRefInstance.set(playerRef.current);

      // add callback when player pauses
      playerRef.current.addEventListener('pause', () => {
        onPaused?.();
      });
    }
  }, []);

  return (
    <Player
      ref={playerRef}
      component={HelloWorld}
      durationInFrames={150}
      fps={30}
      compositionHeight={1080}
      compositionWidth={1920}
      inputProps={data}
      controls
    />
  );
};
```

## Use the component in Angular

To display the Picus player within an Angular template, add your new wrapper component wherever you'd like the player to appear:

```html
<app-player-view [data]="data" (onPaused)="playerPaused()"></app-player-view>
```

This Angular component tag will render the Picus Player, allowing you to pass data or configuration as needed through Angular's data binding.

You're now able to use the [`API`](/docs/player/player) of the player via `this.playerRef()`.

## See also

- [Source code for this documentation](https://github.com/picus-dev/angular-starter)
- [Integration into Svelte](/docs/svelte)
- [Integration into Vue.js](/docs/vue)

<Credits
  contributors={[
    {
      username: 'dothem1337',
      contribution: 'Author',
    },
  ]}
/>
